from bs4 import BeautifulSoup
import re


def pull_data(script):
    return


soup = BeautifulSoup(open('Allen.html'))
for script in soup.find_all(id='source'):
    if 'd2' in str(script):
        speed_list = pull_data(script)

